import 'package:taskmanager/import_links.dart';

emptyList() {
  return const Center(
    child: Text(
      'No Data Found',
      style: TextStyle(color: AppColor.themeColor),
    ),
  );
}
